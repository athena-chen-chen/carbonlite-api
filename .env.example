# Runtime database. Use Neon in shared environments, or the local Postgres
# URL from .env.local.example for local-only development.
DATABASE_URL=
OPENAI_API_KEY=
PORT=3333
NODE_ENV=development
# Optional: backend error reporting
SENTRY_DSN=
RELEASE_VERSION=
